name: Build Doccker Immage

on:
  push:
    branches: 
        - main
jobs:
    docker-build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repository
              uses: actions/checkout@v2

            - name: Build Docker Image
              run: |
                docker build -t employee-portal:v1 .
